# 2026-03-11 — Quarta-Feira

## Resumo do Dia

- Organização de memória diária executada automaticamente às 02:00 UTC
- Consolidação de notas soltas de 2026-03-10
- Atualização de índices de memória

## Atividades

### Torre Dona — Tradução PT-BR quebrou o build (~04:00–04:41 UTC)

**Contexto:** Sessão anterior fez tradução da UI para português (commit `b3c6e03`), seguida de um fix parcial (`4d06965`). Dona fez restart e Atlas perdeu contexto da sessão.

**Problema:** A tradução usou find/replace agressivo que converteu nomes de código (variáveis, tipos, funções, props do React) para português, quebrando a sintaxe:
- `className` → `classNome` (58 arquivos, 3595 ocorrências)
- `useTheme` → `useTema` (import de lib externa next-themes)
- `setTheme` → `setTema`
- `ThemeToggle` → `TemaToggle` (nome de componente exportado)
- `formatUptime()` → `formatTempo ativo()` (espaço no nome da função)
- `Filters` → `Filtrars` (em interfaces, tipos, variáveis)
- `onLogout`/`handleLogout` → `onSair`/`handleSair`
- `getFilteredFiles` → `getFiltraredFiles`
- JSX quebrado: `"<>Carregando...<</span>` (tradução corrompeu tags HTML)

**Fix parcial (commit 4d06965):** Reverteu `classNome` para `className` mas não pegou os outros erros.

**Fix Atlas (commit 18c1b50):** Corrigiu manualmente os 7 erros do Turbopack, mas não detectou os erros profundos (`Filtrars`, `Tema` em variáveis internas, mismatch com store).

**Solução final (commit 1666f00):** Revert completo dos 3 commits de tradução. Código voltou ao estado funcional da paleta visual (commit `a8c91ae`).

**Deploy passou** mas UI continua vazia porque falta variável de ambiente:
- `NEXT_PUBLIC_GATEWAY_PROTOCOL=ws` (sem isso, browser tenta `wss://` mas gateway não tem SSL)
- Variáveis `NEXT_PUBLIC_GATEWAY_HOST` e `NEXT_PUBLIC_GATEWAY_PORT` já existiam no Coolify

**Commits no repo isismor/donasystem:**
- `b3c6e03` — i18n: tradução completa (QUEBROU BUILD)
- `4d06965` — fix parcial: classNome → className
- `18c1b50` — fix Atlas: 7 erros restantes
- `1666f00` — revert completo dos 3 commits de tradução

**Estado às 04:41 UTC:** Deploy funcional, aguardando redeploy com `NEXT_PUBLIC_GATEWAY_PROTOCOL=ws`

## Consolidações do Dia Anterior

Notas de 2026-03-10 foram consolidadas no arquivo principal.

## Torre Dona — Deploy via Coolify (sessão noturna ~00:12–02:09 UTC)

### Infraestrutura descoberta
- Servidor OpenClaw (72.60.241.247): Node 22, sem Docker, 15GB RAM, 171GB disco
- Servidor Coolify (72.61.63.82): VPS separado na Hostinger, Coolify em porta 8000
- São dois servidores distintos. OpenClaw não tem Docker. Coolify faz build remoto.

### Tentativas e erros
1. **Push inicial**: docker-compose com nginx + torre-dona. Nginx crashava: `host not found in upstream "torre-dona"` porque Coolify deployava só o Dockerfile da raiz, não docker-compose
2. **Service Stack no Coolify**: Tentou docker-compose como Service Stack, mas falhou: `path "/data/coolify/services/.../hub" not found` porque Service Stack não clona o repo Git
3. **Solução final**: Recurso tipo **Application** (Dockerfile) no Coolify, apontando para repo `isismor/donasystem`, base directory `/hub`, branch `main`

### Fixes no Dockerfile (hub/Dockerfile)
- Fix 1: Criar pasta `/app/.data` com permissão para SQLite (`mkdir + chown`)
- Fix 2: Copiar `node_modules` completo do stage deps para o runner (better-sqlite3 nativo)
- Fix 3: Adicionar `better-sqlite3` na lista `pnpm.onlyBuiltDependencies` do package.json
- Fix 4: Adicionar `python3 make g++` no base image para compilar nativos
- Fix 5: Remover `basePath: '/hub'` do next.config.js (subdomínio próprio não precisa)

### Estado atual
- App Next.js sobe sem erro: `Ready in 168ms`
- DNS: registro A `hub` → `72.61.63.82` criado no registro.br
- Domínio: `hub.donasystem.com.br`
- Coolify config: Application, Dockerfile, base dir `/hub`, port 3000
- Env vars: AUTH_USER, AUTH_PASS, MC_ALLOW_ANY_HOST
- Landing page (`donasystem.com.br`): restaurada, nginx.conf sem proxy
- Último fix (basePath) ainda não redeployado

### Pendente
- Confirmar se `hub.donasystem.com.br` funciona após redeploy do fix basePath
- Configurar HTTPS (SSL no Coolify)
- Conectar gateway OpenClaw (precisa abrir bind do gateway para IP externo 72.61.63.82)
- OPENCLAW_GATEWAY_HOST precisa ser 72.60.241.247 (não localhost)

### Decisões
- Torre Dona roda em subdomínio próprio (`hub.donasystem.com.br`) em vez de path `/hub`
- Landing page e Torre Dona são recursos separados no Coolify
- basePath removido do Next.js config

### Commits pushados (isismor/donasystem)
- `21d5851` — Push inicial completo (hub/ + docker-compose + nginx proxy)
- `2e19ada` — Fix nginx resolver
- `3d199ea` — Remove proxy do nginx (landing estável)
- `6375354` — Criar pasta .data com permissão
- `46e7fea` — Copiar node_modules com better-sqlite3 nativo
- `17ed57a` — Adicionar better-sqlite3 na lista de build
- `cababda` — Remover basePath

---

*Nota criada automaticamente pelo cron de organização.*
