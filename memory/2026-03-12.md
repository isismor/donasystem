# 2026-03-12 — Quinta-Feira

## Resumo do Dia

- Organizacao de memoria diaria executada automaticamente às 02:00 UTC
- Consolidacao de fragmentos de 2026-03-11
- Rotina de manutencao do sistema de memoria

## Atividades

(Atualizar conforme o dia progride)

### Torre Dona — Sessão 03:16–03:53 UTC (Atlas)

**Contexto:** Isis quer o painel da Torre Dona funcionando com dados reais.

**Diagnóstico da tradução PT-BR:**
- Commit anterior de tradução (b3c6e03) ainda tinha erros residuais no código
- O commit de "reverter" (4d06965) não pegou tudo
- Erros encontrados: `formatTempo ativo()`, JSX quebrado (`"<>Carregando...<</span>`), `useTema`/`TemaToggle`, `Filtrars`, `onSair`
- Solução: revert completo dos 3 commits de tradução (commit 1666f00), código voltou ao estado da paleta visual (a8c91ae)
- Deploy passou após revert

**Diagnóstico da conexão Gateway:**
- Hub precisa de `NEXT_PUBLIC_GATEWAY_PROTOCOL=ws` (sem isso, tenta wss:// e falha porque gateway não tem SSL)
- Isis adicionou a variável no Coolify

**Diagnóstico: hub no Coolify vs servidor local:**
- Hub no Coolify (72.61.63.82) não consegue ler arquivos do OpenClaw (72.60.241.247) — servidores diferentes
- APIs de sessions/cron/memory leem do filesystem local (`~/.openclaw/`)
- Solução: rodar o hub no mesmo servidor do gateway
- Clonei repo, buildei, rodei com `next start` na porta 3001
- API confirmou: 10 cron jobs, 70 sessões, dados reais via cookie auth

**Diagnóstico: firewall do datacenter:**
- Porta 3001 aberta localmente mas BLOQUEADA para tráfego externo
- Porta 18790 também bloqueada
- Apenas portas 18789 (gateway) e SSH estão abertas externamente
- Confirmado pelo erro ERR_CONNECTION_TIMED_OUT no browser da Isis

**Descoberta: Control UI nativo do OpenClaw:**
- O gateway já serve um Control UI embutido em http://72.60.241.247:18789
- SPA (Vite + Lit) com sessões, crons, chat, config, logs, skills, nodes
- Isis acessou mas recebeu "origin not allowed"
- Fix: config.patch adicionando `http://72.60.241.247:18789` nos allowedOrigins
- Gateway reiniciou — aguardando Isis testar

**Estado às 03:53 UTC:** Gateway reiniciando com allowedOrigins corrigidos. Isis vai testar o Control UI nativo.

### Decisões da sessão
- Torre Dona (Mission Control custom) é inviável na infra atual: firewall bloqueia portas extras, servidores separados impedem leitura do filesystem
- Control UI nativo do OpenClaw é a alternativa viável: roda na mesma porta do gateway, sem deploy separado
- Se o Control UI nativo não atender, alternativa: configurar reverse proxy na porta 18789 ou usar Tailscale Serve

### Commits
- `1666f00` — revert completo da tradução (já no repo isismor/donasystem)
- `42c57d5` — consolidação de memória da sessão anterior

---

*Nota criada automaticamente pelo cron de organizacao.*
